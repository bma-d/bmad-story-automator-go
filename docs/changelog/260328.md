# Changelog - 260328

## 260328-23:07:03 - sync story-automator from bmad-fork

### Summary
Ported the standalone story-automator runtime and payload updates from `bmad-fork` commit `9ff25ae5` and rebuilt all packaged binaries.

### Changed
- Synced the standalone Go command handlers with the canonical `bmad-story-automator-go` source subtree.
- Updated workflow payload docs and step files to match the source portability and command-reference fixes.
- Rebuilt packaged binaries for `darwin-amd64`, `darwin-arm64`, `linux-amd64`, and `linux-arm64`.

### Fixed
- Help-path behavior now exits cleanly for `tmux-wrapper`, `monitor-session`, and `orchestrator-helper`.
- Invalid numeric CLI inputs now fail clearly instead of silently coercing to zero.
- Marker handling, parse-output validation, frontmatter parsing, and story/sprint completion logic now match the synced source behavior.

### Files
- `source/cmd/story-automator/*`
- `payload/_bmad/bmm/workflows/4-implementation/story-automator-go/data/*`
- `payload/_bmad/bmm/workflows/4-implementation/story-automator-go/steps-c/*`
- `payload/_bmad/bmm/workflows/4-implementation/story-automator-go/steps-v/*`
- `artifacts/story-automator/bin/*`

### QA Notes
- Verify `artifacts/story-automator/bin/darwin-arm64/story-automator tmux-wrapper --help` prints usage and exits cleanly.
- Verify `artifacts/story-automator/bin/darwin-arm64/story-automator monitor-session --help` prints usage immediately instead of hanging.
- Verify invalid numeric input fails clearly, for example `validate-story-creation check 5.3 --before nope --after 1` and `orchestrator-helper marker create ... --remaining nope`.
